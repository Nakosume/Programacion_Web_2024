import { GIF_ACTIONS } from '../const/GifActions'

export const reducerGIF = (fact, action) => {
  switch (action.type) {
    case GIF_ACTIONS.SET_GIF:{
      return action.payload }
    default:
      // eslint-disable-next-line no-undef
      return gif
  }
}
