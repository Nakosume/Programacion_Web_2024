// Reducer & functions

export const GifContextProvider = ({ children }) => {
  return (
    {/* <CatContext.Provider value={{
      fact,
      catImageUrl,
      loading,
      newFact
    }}
    >
      {children}
    </CatContext.Provider> */}
  )
}
